/**
 * Epic Fight's animation engine: clips and joint transforms hold pose data;
 * animators play it for patched entities; animation types add state windows,
 * root motion, hit phases, events, and transition behavior.
 */
package yesman.epicfight.api.animation;
