/**
 * Platform entry points and the bridges that assemble Epic Fight's subsystems.
 *
 * <p>On Fabric, begin with {@code EpicFightFabricInitializer} for common/server
 * setup and {@code EpicFightFabricClientInitializer} for client-only setup.
 * {@code EpicFightMod} still contains shared constants and legacy Forge-facing
 * helpers, but it is not the entry point selected by {@code fabric.mod.json}.</p>
 */
package yesman.epicfight.main;
