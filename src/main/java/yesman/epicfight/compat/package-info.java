/**
 * Optional integrations with other mods. Each integration must remain guarded
 * by a loader check so its dependency's classes are never resolved when that
 * mod is absent.
 */
package yesman.epicfight.compat;
