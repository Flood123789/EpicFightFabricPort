/**
 * High-level client coordinators. {@code ControlEngine} translates physical
 * input into combat actions and packets; {@code RenderEngine} coordinates HUD,
 * patched renderers, camera-facing effects, and render-event integration.
 */
package yesman.epicfight.client.events.engine;
