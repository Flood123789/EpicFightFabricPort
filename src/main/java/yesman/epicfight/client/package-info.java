/**
 * Client-only input, cameras, screens, particles, animation presentation, and
 * rendering. Code here must not be loaded by a dedicated server.
 *
 * <p>{@code ClientEngine} owns the main client coordinators: its control engine
 * turns physical input into combat requests, while its render engine presents
 * synchronized combat and animation state.</p>
 */
package yesman.epicfight.client;
